<%= form_for(@level) do |f| %>
  <% if @level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@level.errors.count, "error") %> prohibited this level from being saved:</h2>

      <ul>
      <% @level.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tiles %><br />
    <%= f.text_field :tiles %>
  </div>
  <div class="field">
    <%= f.label :objects %><br />
    <%= f.text_field :objects %>
  </div>
  <div class="field">
    <%= f.label :enemies %><br />
    <%= f.text_field :enemies %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
